# NOTE: python3-gps must be installed via apt, not pip:
# sudo apt install -y gpsd gpsd-clients python3-gps
# pinned to 0.2.93 — librtlsdr 2.0.2 (Osmocom fork on Trixie)
# has different API than newer pyrtlsdr expects (missing rtlsdr_set_dithering)
pyrtlsdr==0.2.93
numpy>=1.24,<3
requests
python-dotenv
geopandas
fiona
shapely
scapy
bleak
aiohttp
